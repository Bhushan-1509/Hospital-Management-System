import javax.swing.*;
import java.awt.*;

public class LoginBtn extends JButton {
    public LoginBtn(String text){
        setSize(300,400);
        setText(text);
        setBackground(Color.white);
    }
}
